DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
ADMIN_PASSWORD_HASH=""
SESSION_SECRET=""
CRON_SECRET=""
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="Social Nerds <you@gmail.com>"
NEXT_PUBLIC_SITE_URL="https://your-domain.com"

# Cloudinary — admin-published gallery media and event posters.
# The secret is server-only; the browser only ever receives a per-upload
# signature. If it ever leaks, rotate it in the Cloudinary console immediately:
# the pair is full admin access to the account and can delete every asset.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
